def latestSuccess = env.LATEST_SUCCESS

pipeline {
  agent any
  stages {
    stage("Tests") {
      steps {
        echo Integer.toString(currentBuild.changeSets.size())
      }
    }
  }
}

def checkLatestSuccess(latestSuccess) {
    if(latestSuccess != NULL) {

    }
}
